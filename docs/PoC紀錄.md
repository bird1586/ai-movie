# PoC 紀錄

每次付費執行後補一節：實際花了什麼、卡在哪、下次怎麼改。規劃與步驟見 `worker/poc/README.md`。

## 2026-09-24 第 3 次：修正運鏡 + 10 秒仙俠對決（一鍵腳本，NT$6.84）

**結果：一次開機跑 5 支 5 秒影片**，全部都有運鏡。選兩支接成 10 秒成片 `data/poc/xianxia_10s.mp4`
（1280×704、24 fps、242 幀、10.08 s）：前 5 秒是山頂雙劍交鋒，慢動作環繞；後 5 秒是兩人御劍穿過雲海，跟拍。

| 鏡頭 | seed | 首→中／首→尾幀差（/255） | 評語 |
|---|---|---|---|
| `opening_pan`（第 2 次，對照） | 42 | 1.7 / 1.8 | 靜止 |
| `opening_pan_v2` | 42 | 18.0 / 29.0 | 有橫搖，但畫面還是偏綠 |
| `xianxia_clash` | 7 | 25.9 / 31.5 | 光弧很華麗，但劍看起來像長棍 |
| `xianxia_clash` | **2024** ✅ | 23.4 / 30.2 | 劍尖相抵、火花，人物、服裝最乾淨 |
| `xianxia_flight` | 7 | 66.0 / 80.5 | 飛行感強，但男角沒有踩在劍上 |
| `xianxia_flight` | **2024** ✅ | 63.0 / 85.0 | 兩人都在劍上，夕陽雲海 |

| 項目 | 值 |
|---|---|
| 機器 | RTX 5090，節點 `b4d0f38c`，US$0.5994/h（≈ NT$18.9/h） |
| 開機 | 建立 → RUNNING **26.7 s**（官方範本已快取） |
| 第 1 個鏡頭（冷） | **591.8 s**，比熱機多約 420 s（同樣從 vault 載入，`72ca950b` 只多 90 s） |
| 其餘 4 個（熱） | 168.5–170.4 s，VRAM 峰值 21.8–25.0 GB |
| 計費 | 約 1308 s；餘額 NT$79.14 → 72.30，**NT$6.84**（stop 後 5 秒查的，可能還沒全部入帳） |

### 靜態畫面的原因

1. 第 2 次的提示詞把 "The camera slowly pans…" 放在一長串場景描述**後面**，5B 模型主要聽前面的字。
2. 負面提示詞裡另外加了「镜头抖动」（原本想避免手震），連同「静态」「静止」，模型乾脆讓鏡頭完全不動。

v2 把運鏡放在第一句，並拿掉「镜头抖动」，畫面變化量就從 1.8 升到 29（見上表）。

### 流程檢討與改進

1. ✅ **一鍵腳本 `gputw_session.py`**：這次從開機到 stop 完全沒有人介入，閒置只有開機的 27 s 和最後下載。
   第 2 次 17 分鐘裡只有 6.5 分鐘在算，這次 22 分鐘裡約 21 分鐘都在算。
2. ✅ **多鏡頭同一次開機**：`run_poc.py` 可以一次給多個 shot，只有第一個要載入模型。
3. ⚠️ **冷載入跟節點有關**：`b4d0f38c` 載入模型要約 7 分鐘（≈ NT$2.2）。下次可以先試 `72ca950b`（貴 23%，但載入快 5 倍）。
   只跑 1–2 個鏡頭時，選載入快的節點比較划算；鏡頭多時，選單價便宜的比較划算。
4. **先跑 1 個 seed 當試拍**：這次兩個 seed 都能用，但各佔 170 s。畫面方向確定後，每個鏡頭只跑 1 個 seed，費用少一半。
5. **鏡頭之間的銜接**：現在是硬切。之後可以用 TI2V 把前一鏡的最後一格當下一鏡的 start_image，或加 0.3 s 的交叉淡化。
6. **把 prompt 排入佇列**：`run_poc.py` 現在是跑完一支、下載完才送下一支，GPU 會空等下載（每支約 3–5 s）。
   改成開頭就把所有 prompt 送進 ComfyUI 佇列，再依序下載。
7. **畫面偏綠**（opening_pan）：提示詞加上 "bright golden hour, vivid natural colors" 再試。

## 2026-09-24 第 2 次：官方 ComfyUI 範本 + vault 模型 — 成功（NT$6.47）

**結果：兩支 5 秒影片，1280×704、121 幀、24 fps、5.04 s**，存在本機 `data/poc/`（gitignored）。
畫質和光線都不錯（霧、光束、瀑布、飛鳥），**但鏡頭幾乎沒動**：首尾幀平均只差 1.4–1.8/255，
只有鳥在動，沒有要的橫搖。

| 項目 | 值 |
|---|---|
| 範本 | 官方 `gputw/comfyui`（ComfyUI **0.34.0**，有 `Wan22ImageToVideoLatent`），Web UI 8080 |
| 機器 | RTX 5090 32GB，節點 `72ca950b`，US$0.7356/h（約 NT$23.2/h） |
| 模型 | 直接用 vault `models/<類別>/` 裡的檔案，範本自動看得到，不用設路徑 |
| 開機 | 02:17:17 建立；第一次查詢時（02:27）早已 RUNNING。**沒有拉映像的等待**（確切開機秒數沒量到） |
| seed 42（冷） | **240.3 s**，VRAM 峰值 23.8 GB |
| seed 1234（熱） | **150.5 s**，VRAM 峰值 20.4 GB → 冷啟動多 **89.8 s**（從 vault 載入模型） |
| 單位成本 | 熱機每 1 秒成片約 **29.9 GPU 秒 ≈ NT$0.19** |
| 實際扣款 | 餘額 NT$85.61 → **NT$79.14（NT$6.47，約 17 分鐘）**，其中真正在算的只有約 6.5 分鐘 |
| 執行方式 | 本機 `run_poc.py --comfy https://8080-<id>.gputw.ai`（帶 access-token 換到的 cookie），影片經 `/view` 下載 |

17 分鐘裡大約 10 分鐘是在等人設定 API key（auto mode 擋住讀取外掛儲存的 key，改由使用者放進 `~/.config/gputw/key`）。
key 已經放好，下次不會再卡在這裡。

### 優化建議（下次省時省錢、畫質更好）

1. **全流程自動化**：建立 → 輪詢 RUNNING → 拿 access token → 跑 `run_poc.py` → stop，寫成一支腳本，
   人不在旁邊也能跑。這次真正在算的只有 6.5 分鐘，理想上一次 PoC 約 **NT$3–4**。
2. **一次開機跑多個鏡頭**：冷啟動（載入模型）只多 90 s ≈ NT$0.6，但每開一次機就要付一次。
   把一整批鏡頭排進同一次開機，不要一個鏡頭開一次。
3. **不需要另外做模型快取**：vault 載入 16.9 GB 只要約 90 s，先複製到 /workspace 省下的時間比複製本身還少。
   官方範本本身就已快取在節點上。stop（不 delete）執行個體可以保留 /workspace，也能在同一台 restart。
4. **鏡頭運動（畫質最大的問題）**：5B 純文字生影片不太聽「pan」的指令。可以試：
   - 提示詞開頭直接寫鏡頭動作（如 "Camera pans right: …"、"tracking shot"），並把「静止、静态」留在負面提示詞裡（已經有）；
   - 改用圖生影片（TI2V 的 start_image）：先生成一張超寬的景，再讓它平移；
   - 或加 camera-motion LoRA／改用 A14B（貴約 5 倍，見可行性評估 C-1）。
   - 另外畫面偏暗、偏綠，可以在提示詞加 "bright golden hour, vivid colors, wide establishing shot"。
5. **挑便宜的節點**：這次最便宜的 5090 節點都被占用，只能用 $0.7356。4090 有空時更便宜。

## 2026-09-24 第 1 次：5 秒片頭（Wan2.2 TI2V-5B）— 失敗，卡在拉映像

**結果：沒有產出影片。** 執行個體在 PULLING_IMAGE 卡了 45.8 分鐘，之後平台把它標成 FAILED，
沒有給失敗原因，log API 回 500。隨即 stop，確認 0 台 RUNNING。

| 項目 | 值 |
|---|---|
| 映像 | `ghcr.io/bird1586/ai-movie-worker:fef3cf688df7`（壓縮後 6.16 GB，19 層，最大一層 4.01 GB） |
| 機器 | RTX 5090 32GB，節點 `b4d0f38c`，US$0.5994/h（約 NT$18.9/h），頻寬 100 Mbps（內含的方案） |
| 參數 | `args: ["poc"]`、`OUTPUT_DIR=/workspace/outputs`、Web UI 8188、sshEnabled |
| 時間軸 (UTC) | 01:21 開始下載模型到 vault → 01:26 完成 → 01:27:17 建立執行個體 → 02:13 FAILED → stop |
| 花費 | 餘額 NT$100 → **NT$85.61（花了 NT$14.39）**，幾乎都是拉映像的 GPU 計費 |
| /vault | 3 個模型 16.9 GB，已就位，**保留**（每月約 NT$34）；下次直接用 |

### 經過

1. `download-model-to-vault` 伺服器端下載 3 個模型（HF → vault）：**10 GB 約 5 分鐘（約 33 MB/s）**，不開 GPU，這一步很順。
2. RTX 4090 售完，改用 5090 裡最便宜的節點。
3. 事件只有 `Scheduled` → `Pulling image`，45 分鐘都沒有任何 Failed 或 BackOff 事件，也看不到下載進度。
4. 從本機（Oracle）抓 GHCR 同一層實測約 13 MB/s，整個映像約 8 分鐘可以抓完；
   GPU 節點 45 分鐘還沒拉完，有效速度 < 2.3 MB/s。所以瓶頸在**節點到 GHCR 的線路**（或解壓），不是映像壞掉。
5. 平台大約在 45 分鐘時放棄（推測有部署逾時），狀態變 FAILED，但沒有 failureReason。

### 教訓與優化（依優先順序）

1. **不要讓 GPU 機器去拉大型自帶映像。** 拉映像的時間全部按 GPU 單價計費，而且速度無法預測。
   vault 的伺服器端下載快又不計 GPU 費用，應該把「大東西」都走 vault。
2. **下次改用 GPUtw 官方 ComfyUI 範本**（`gputw/comfyui`，templateId 用 `list-templates` 查，不要寫死）。
   官方範本很可能已經快取在節點上，開機不用拉映像。
   - 要先確認：範本的 ComfyUI 版本有沒有 `Wan22ImageToVideoLatent` 節點（v0.3.46 以後才有），
     以及模型路徑怎麼對到 `/vault/models`（`extra_model_paths.yaml` 或 symlink）。
   - Web UI 是 8080。`run_poc.py` 可以在**本機**跑，用 `--comfy https://<web UI 網址>` 直接呼叫 API，
     容器裡不用放任何東西。影片也用 `/view` 從同一個網址下載回本機。
   - 注意：`run_poc.py` 的 `probe` 會用 `--output-dir` 找檔案，本機跑時要改成下載後再 probe。
3. 如果還是要用自帶映像（之後需要 CosyVoice、LatentSync 時）：
   - 映像只放系統套件和程式碼（目標 < 1 GB），torch 和兩個 venv 打包成 tar 放 vault，
     開機時直接從 vault 用（或解壓到 /workspace）。
   - 把 4 GB 的單一層拆成多層，讓下載可以平行。
   - 先開一次、拉完映像後就一直用**同一個節點**（映像會快取在節點上）。這次沒拉完，快取狀況不明。
4. **設停損時間。** 部署時就定好上限（例如拉映像 15 分鐘沒進 STARTING 就 stop），不要一路等到平台逾時。
   這次多等的 30 分鐘約 NT$9.5。
5. 開機前先確認帳戶的 SSH 金鑰已經加到 GPUtw console。這次也不確定，所以輸出改寫到 `/workspace`，
   打算用 Web UI 的 `/view` 下載。
6. `run_poc.py` 的 `RATE_NT_PER_H` 寫死成 4090 的費率。改用其他卡時，報告裡的費用要自己換算。

### 下次 PoC 預估（改用官方範本）

開機約 1–3 分鐘，加上模型載入和兩個 seed 各約 5 分鐘（5090），總共約 15 分鐘 ≈ **NT$5**。
